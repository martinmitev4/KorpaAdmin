@{
}
@model List<KorpaAdmin.Models.Domain.Delivery_orders>


<div class="container">
    @*<div class="row">
        <a asp-controller="Order" asp-action="ExportAllOrders" class="btn btn-info">Export Orders</a>
    </div>
    *@

    <div class="row m-5">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        #
                    </th>
                    <th>
                        Customer details
                    </th>
                    <th>
                        Number of products
                    </th>
                    <th>
                        Address
                    </th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count(); i++)
                {
                    var item = Model[i];
                    <tr>
                        <td>@(i + 1)</td>
                        <td>
                            @item.Customer.FirstName
                        </td>

                        <td>
                            @item.FoodInOrders.Count()
                        </td>
                        <td>
                            @item.Address
                        </td>
                        <td>
                            |
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">View Order</a>
                        </td>
                        <td>
                            |
                            <a asp-action="CreateInvoice" asp-route-id="@item.Id" class="btn btn-info">Create Invoice</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>